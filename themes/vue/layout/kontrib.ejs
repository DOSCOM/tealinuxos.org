
<div class="content <%- page.type ? page.type + ' with-sidebar' : '' %> <%- page.type === 'panduan' ? page.path.replace(/.+\//, '').replace('.html', '') + '-panduan' : '' %>">
    <% if (page.type) { %>
      <%- partial('partials/ad') %>
    <% } %>
    <% if (page.title.trim()) { %>
      <h1 class="tengah"><%- page.title %><%- page.type === 'examples' ? ' Example' : '' %></h1>
    <% } %>

    <p>
        <%- page.subtitle %>
    </p>
    <span class="breathe"></span>

    <p class="">
        <a class="button white tombol" href="/download/">ijo</a>
        <a class="button white tombol" href="/panduan/">putih</a>
    </p>

    <%- page.content %>

    <% if (page.type === 'panduan') { %>
      <div class="guide-links">
        <% site.pages.find({ type: page.type, order: page.order - 1 }).each(function (page) { %>
          <span>← <a href="<%- url_for(page.path) %>"><%- page.title %></a></span>
        <% }) %>
        <% site.pages.find({ type: page.type, order: page.order + 1 }).each(function (page) { %>
          <span style="float:right"><a href="<%- url_for(page.path) %>"><%- page.title %></a> →</span>
        <% }) %>
      </div>
    <% } %>

    <div class="footer">
      Menemukan kesahalan atau ingin berkontribusi pada dokumentasi ini ?
      <a href="https://github.com/doscom/tealinuxos.org/blob/master/src/<%- page.path.replace(/\.html$/, '.md') %>" target="_blank"><br>
        Sunting halaman ini secara langsung di Github
      </a>
    </div>
</div>
